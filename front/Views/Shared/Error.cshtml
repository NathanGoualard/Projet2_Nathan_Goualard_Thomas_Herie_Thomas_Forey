@model front.Models.ErrorViewModel

<h1>Erreur</h1>

<p>Une erreur est survenue.</p>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}
